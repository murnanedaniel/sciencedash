{
  "dashboard_url": "http://localhost:3000",
  "host": "perlmutter",

  "_comment_projects": "One entry per project active on this host. projectId is the ScienceDash project's cuid; sessionName is the tmux session that runs Claude Code (convention: sd-<projectId>); repo is the absolute path to the project's local repo (so 'revive_session' knows where to cd).",
  "projects": [
    {
      "projectId": "<your-project-cuid>",
      "sessionName": "sd-<your-project-cuid>",
      "repo": "/path/to/your/project/repo"
    }
  ]
}
